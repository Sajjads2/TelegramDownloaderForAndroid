package com.app.ui

import androidx.compose.material3.*
import androidx.compose.runtime.*

@Composable
fun HomeScreen() {

    Column {

        Text("TDLib Downloader")

        Button(onClick = {}) {
            Text("Scan")
        }

        Button(onClick = {}) {
            Text("Download")
        }
    }
}
